# Dump Service (should expose metrics)
apiVersion: v1
kind: Service
metadata:
  name: dump-cassandra-eth
  namespace: search
  labels:
    monitoring: default-monitor
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: dump-cassandra-eth
---

# Dump Deployment
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: search
  name: dump-cassandra-eth
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      namespace: search
      labels:
        app: dump-cassandra-eth
    spec:
      containers:
      - env:
        - name: CHAIN_FAMILY
          value: ETHEREUM
        - name: CASSANDRA_HOSTS
          value: elassandra
        - name: CS_LOG_LEVEL
          value: INFO
        - name: KAFKA_BROKERS
          value: kafka-broker:9092
        - name: JAVA_OPTS
          value: -Xms3g -Xmx3g
        image: cybernode/chain-dump-cassandra-ethereum:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "1.5"
            memory: "3.75Gi"
          limits:
            cpu: "3"
            memory: "3.75Gi"
        ports:
        - containerPort: 8080
        name: dump-cassandra-eth
      restartPolicy: Always