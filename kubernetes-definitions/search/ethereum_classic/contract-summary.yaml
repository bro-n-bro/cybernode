# Service (should expose metrics)
apiVersion: v1
kind: Service
metadata:
  name: contract-summary-etc
  namespace: search
  labels:
    monitoring: default-monitor
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: contract-summary-etc
---

# Deployment
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: search
  name: contract-summary-etc
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      namespace: search
      labels:
        app: contract-summary-etc
    spec:
      containers:
      - env:
        - name: CHAIN
          value: ETHEREUM_CLASSIC
        - name: CASSANDRA_HOSTS
          value: elassandra
        - name: CS_LOG_LEVEL
          value: INFO
        - name: KAFKA_BROKERS
          value: kafka-broker:9092
        image: cybernode/chain-contract-summary-ethereum:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        name: contract-summary-etc
      restartPolicy: Always