# Dump Service (should expose metrics)
apiVersion: v1
kind: Service
metadata:
  name: dump-cassandra-btc
  namespace: search
  labels:
    monitoring: default-monitor
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: dump-cassandra-btc
---

# Dump Deployment
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: search
  name: dump-cassandra-btc
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      namespace: search
      labels:
        app: dump-cassandra-btc
    spec:
      containers:
      - env:
        - name: CHAIN_FAMILY
          value: BITCOIN
        - name: CASSANDRA_HOSTS
          value: elassandra
        - name: CS_LOG_LEVEL
          value: INFO
        - name: KAFKA_BROKERS
          value: kafka-broker:9092
        - name: JAVA_OPTS
          value: -Xms3g -Xmx3g
        - name: KAFKA_LISTENER_MAX_POLL_RECORDS
          value: "100"
        - name: CASSANDRA_MAX_CONNECTIONS_LOCAL
          value: "32768"
        - name: CASSANDRA_MAX_CONNECTIONS_REMOTE
          value: "32768"
        image: cybernode/chain-dump-cassandra-bitcoin:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "3"
            memory: "3.75Gi"
          limits:
            cpu: "3"
            memory: "3.75Gi"
        ports:
        - containerPort: 8080
        name: dump-cassandra-btc
      restartPolicy: Always