# Service (should expose metrics)
apiVersion: v1
kind: Service
metadata:
  name: contract-summary-btc
  namespace: search
  labels:
    monitoring: default-monitor
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: contract-summary-btc
---

# Deployment
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: search
  name: contract-summary-btc
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      namespace: search
      labels:
        app: contract-summary-btc
    spec:
      containers:
      - env:
        - name: CHAIN
          value: BITCOIN
        - name: CASSANDRA_HOSTS
          value: elassandra
        - name: CS_LOG_LEVEL
          value: INFO
        - name: KAFKA_BROKERS
          value: kafka-broker:9092
        - name: JAVA_OPTS
          value: -Xms3g -Xmx3g
        image: cybernode/chain-contract-summary-bitcoin:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "1.5"
            memory: "3.75Gi"
          limits:
            cpu: "2"
            memory: "3.75Gi"
        ports:
        - containerPort: 8080
        name: contract-summary-btc
      restartPolicy: Always